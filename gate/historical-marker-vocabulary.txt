# Historical-marker vocabulary for Rule 80 (s2c_callback_signal_historical_only_in_authority).
# Each non-comment, non-empty line is one regex alternative.
# Combined as `marker1|marker2|...` and used as an awk regex against the ±5-line window
# around a `S2cCallbackSignal` mention.
#
# Authority: Authority-Surface Cleanup migration plan (2026-05-19) — Wave 2 externalisation.
# Adding a new marker: append a line; rerun `bash gate/check_architecture_sync.sh`.
# Removing a marker: delete a line; corpus instances of that marker become non-exempt.
#
# Footnote-style markers (post-Wave-3): `[hist→docs/logs/...]` is the canonical pointer
# format for historical references in normative documents.

historical
deleted
refactored
refactored from
amendment
amendments
superseded
retired
pre-migration
rc[0-9]+-unification
rc[0-9]+ unif
unifies
unified
prior parallel
forClientCallback
2026-05-16 design
\[hist→docs/logs/[^]]*\]
