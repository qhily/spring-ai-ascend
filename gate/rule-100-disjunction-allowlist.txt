# Rule 100 — kernel_implementation_disjunction_truth allow-list.
#
# Rules whose kernel explicitly declares "EITHER / OR" semantics — meaning the
# matching gate-script implementation uses `||` (disjunction) on the asserted
# conditions, and BOTH the CLAUDE.md kernel block AND the rule card MUST
# carry the explicit EITHER/OR connective wording.
#
# Format: one rule id (engineering-rule number) per line. Lines starting with
# `#` are comments.
#
# Authority: ADR-0085 (rc11 wave); rc10 post-corrective review P1-3 closure.
#
# Initial allow-list (rc11):
#   96 — kernel_deferred_clause_coherence (kernel block OR card)
#
# Future additions: J-γ sweep may surface Rule 48 (yaml schema OR grandfather
# entry), Rule 69 (active heading OR deferred reference), Rule 95 (catalog
# row OR internal mark). Each addition requires kernel + card to explicitly
# declare EITHER/OR wording before the rule id lands here.

96
