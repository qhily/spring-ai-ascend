# Baseline-snapshot historical-marker vocabulary for Rule 82 (baseline_metrics_single_source).
# Each non-comment, non-empty line is one regex alternative.
# A line in README.md / gate/README.md that contains a `<number> <phrase>` claim is
# exempted from numeric-agreement against the structured baseline_metrics block if
# any of these markers appears on the same line.
#
# Authority: Authority-Surface Cleanup migration plan (2026-05-19) — Wave 2 externalisation.
# rc-version-specific tokens (`rc[N] baseline`, `pre-rc[N]`) remain in this vocabulary
# during Wave 2; Wave 3 strips them once the corpus prose is scrubbed of rcN literals.

historical
rc[0-9]+ baseline
pre-rc[0-9]+
previous
prior
deprecated
superseded
retired
formerly
was [0-9]
\[snapshot→docs/logs/baselines/[^]]*\]
