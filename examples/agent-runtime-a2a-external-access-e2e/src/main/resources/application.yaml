debug: false

spring:
  application:
    name: agent-runtime-a2a-external-access-e2e

server:
  port: ${SAA_EXTERNAL_ACCESS_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
